# frontend/Dockerfile.dev
# Dev Dockerfile for SafeShipping Vite frontend (hot reload + debug mode)

FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install deps
COPY package*.json ./
RUN npm install

# Copy full frontend code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server
CMD ["npm", "run", "dev"]
